version: '3.8'

services:
  server:
    build:
      context: ./server
      dockerfile: Dockerfile
    image: scandrop-server:latest
    container_name: scandrop-server
    ports:
      - "3000:3000"
    env_file:
      - ./server/.env
    restart: unless-stopped
